# DOTS Daily Monorepo

This repository is split into two folders:

- `client/` — front-end (Vite + React)
- `server/` — back-end (placeholder)

Run the client:

```
cd client
npm install
npm run dev
```
